# VS Code Configuration

This folder contains VS Code workspace settings to properly configure:
- Python interpreter (virtual environment)
- Test discovery (pytest)
- Test execution

## Settings Configured

- **Python Interpreter**: Uses `.venv/Scripts/python.exe`
- **Testing Framework**: pytest
- **Test Root**: `tests/` directory
- **Test Pattern**: `test_*.py` files
